# Prompt: 单方向综述 SVG 作图结构化
# 建议模型: DEEPSEEK_MODEL
# 用途: 基于 direction_records 和单方向综述 Markdown 生成单方向作图 JSON
# 输入: topic, direction_info, direction_records, literature_review_md, optional key formulas/figures
# 输出: plot_ready JSON

你是“单方向综述作图结构化助手”。

任务：
请把当前方向的文献信息整理成适合渲染 SVG 综述图的 JSON。输出只服务作图，不要写自然语言综述。

研究主题：
{topic}

当前方向：
```json
{direction_info_json}
```

方向内 records：
```json
{direction_records_json}
```

单方向综述 Markdown：
```markdown
{literature_review_md}
```

关键公式与图表补充信息：
```json
{key_formulas_figures_json}
```

硬性要求：
1. 输出必须是严格 JSON，不要附加解释文字。
2. 所有作图文字使用中文。
3. 禁止输出未解释的英文术语、英文缩写或裸变量符号。
4. 如果必须出现符号或缩写，必须在同一条中立即解释，并放入 symbol_glossary_cn。
5. 每个方框最多 3 条短句，每条尽量不超过 28 个汉字。
6. 不要重复单方向综述中的长段落，只保留适合作图的短句。
7. baseline_or_mainline 选择最能代表该方向逻辑主线的论文；如果没有明显主线，选择代表性最高的论文。
8. comparison_rows 中描述相对主线的差异；相同处写“同主线”。
9. 必须忠实于 direction_records 和 literature_review_md，不要编造。

输出 JSON 结构：
{
  "direction_id": "",
  "direction_name_cn": "",
  "figure_title_cn": "",
  "core_problem_box": ["短句1", "短句2"],
  "baseline_or_mainline": {
    "paper_id": "",
    "citation_cn": "作者等（年份）",
    "input_box_cn": ["短句1", "短句2"],
    "method_box_cn": ["短句1", "短句2"],
    "output_box_cn": ["短句1", "短句2"]
  },
  "comparison_rows": [
    {
      "paper_id": "",
      "citation_cn": "作者等（年份）",
      "input_diff_cn": ["短句1"],
      "method_diff_cn": ["短句1"],
      "output_diff_cn": ["短句1"]
    }
  ],
  "method_evolution_box": ["短句1", "短句2", "短句3"],
  "formula_boxes": [
    {
      "formula_id": "",
      "formula_latex": "$$...$$",
      "meaning_cn": "一句话说明公式用途"
    }
  ],
  "research_gap_box": ["短句1", "短句2", "短句3"],
  "symbol_glossary_cn": [
    {
      "symbol": "",
      "meaning_cn": ""
    }
  ],
  "self_check": {
    "all_text_cn_or_explained": true,
    "box_length_ok": true,
    "no_unexplained_symbol": true,
    "notes": ""
  }
}
