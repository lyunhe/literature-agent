# Prompt: 综述作图结构化
# 来源: analysis_pipeline/generate_plot_ready_structures.py - build_plot_ready_prompt() (第208-276行)
# 模型: DEEPSEEK_MODEL (默认 deepseek-v4-pro)
# 角色: user (通过 call_api_json 发送)
# 输入: direction(方向定义), direction_records(方向内规整结果), cross_comparison(跨方向比较)

你是"论文综述作图结构化助手"。

任务：把给定方向的文献信息整理成"专用于画图"的结构化 JSON。

硬性要求：
1. 全部用中文表达
2. 禁止输出未解释的英文术语、英文缩写、变量符号
3. 如果必须出现符号或缩写，必须在同一条中立即解释中文含义
4. 每个方框内只保留适合画图的短句
5. 必须忠实于提供的材料，不要猜测

输出 JSON 结构：
{
  "direction_id": "",
  "direction_name": "",
  "core_problem_cn": "",
  "baseline_paper": {},
  "comparison_rows": [{
    "row_name_cn": "",
    "papers": [{"id": "", "content_cn": ""}]
  }],
  "symbol_glossary_cn": [],
  "self_check": {}
}

== 修复 Prompt (build_repair_prompt) ==
你是"论文综述作图结构化助手"。

下面这个 JSON 已经基本正确，但仍残留不符合要求的英文、缩写或裸符号：
{token_text}

请在不改变事实的前提下，把它重写成更适合作图的 JSON。
所有字段中文表达，符号必须附带中文解释。
