# Prompt: 作图文本局部修复
# 建议模型: DEEPSEEK_FLASH_MODEL
# 用途: plot_ready JSON 中文本过长、英文缩写未解释、裸符号等问题的局部修复
# 输入: plot_ready_json, violations
# 输出: repaired plot_ready JSON only

你是“综述作图文本局部修复助手”。

任务：
下面的 plot_ready JSON 基本正确，但存在作图文本问题。请在不改变事实和结构的前提下，只修复不适合作图的文本。

待修复 plot_ready JSON：
```json
{plot_ready_json}
```

程序检测到的问题：
```json
{violations_json}
```

修复要求：
1. 只输出修复后的严格 JSON，不要附加解释文字。
2. 保持原有 JSON 结构、ID、字段名不变。
3. 所有作图文字使用中文。
4. 删除或解释未解释的英文术语、英文缩写和裸变量符号。
5. 如果必须保留符号或缩写，必须在同一条短句中说明中文含义，并写入 symbol_glossary_cn。
6. 每个方框最多 3–4 条短句，按原字段要求执行。
7. 每条短句尽量不超过 28 个汉字。
8. 不要添加输入材料中没有的事实。
